# Auth - REQUIRED for session persistence across refreshes
# Generate with: openssl rand -hex 32
COOKIE_SECRET=your-random-secret-here

# Google OAuth
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
ALLOWED_EMAILS=admin@alpha.school,teacher@alpha.school

# SpacetimeDB owner tokens (required for admin operations)

# Maincloud token (for local/staging environments)
# Get your token: spacetime login show --token
VITE_SPACETIMEDB_TOKEN=your-maincloud-owner-token-here

# EC2 token (for production environment)
# Get from EC2: ssh math-raiders "grep SPACETIMEDB_TOKEN /home/ubuntu/mathraiders-worker/.env | cut -d'=' -f2"
VITE_SPACETIMEDB_TOKEN_EC2=your-ec2-owner-token-here
