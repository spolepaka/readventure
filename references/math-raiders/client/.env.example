# SpacetimeDB connection
VITE_SPACETIMEDB_HOST=ws://localhost:3000
VITE_MODULE_NAME=math-raiders

# Admin mode (set to 'true' to authenticate as module owner for admin operations)
# For local dev: set to 'true' to test admin features
# For production: must be 'false' (students should not authenticate)
VITE_ADMIN_MODE=false

# SpacetimeDB owner token (only needed if VITE_ADMIN_MODE=true)
# Get your token: spacetime login show --token
# VITE_SPACETIMEDB_TOKEN=your-token-here

# TimeBack OAuth (for grade fetching)
# Production credentials (get from TimeBack team)
VITE_TIMEBACK_CLIENT_ID=your-client-id
VITE_TIMEBACK_CLIENT_SECRET=your-client-secret
